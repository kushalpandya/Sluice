{
  "name": "sluice",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "Sluice: a small self-hostable report-triage platform — a Cloudflare Worker (D1 + R2) that receives in-app problem reports (with attachments) for pull-based triage, plus a Preact web UI for reviewing and promoting them to GitHub issues.",
  "scripts": {
    "dev": "vite",
    "dev:api": "cd worker && wrangler dev",
    "build": "vite build",
    "deploy:web": "vite build && wrangler pages deploy dist --project-name sluice-ui --branch production",
    "deploy:api": "cd worker && wrangler deploy",
    "tail": "cd worker && wrangler tail",
    "typecheck": "tsc -p worker/tsconfig.json"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "bulma": "^1.0.4",
    "preact": "^10.26.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@preact/preset-vite": "^2.10.1",
    "sass": "^1.101.0",
    "typescript": "^5.6.0",
    "vite": "^6.3.5",
    "wrangler": "^4.107.0"
  }
}
